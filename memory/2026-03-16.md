
## Auth fix session (sysnet-org)
- Checked repo HEAD: `3845851`.
- Verified required env vars in `frontend/.env`: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` are set.
- Found `NEXT_PUBLIC_SITE_URL` missing in `frontend/.env`.
- Attempt to query Vercel deployments via CLI failed due TLS/network error loading OIDC config.
- Browser control restored and confirmed attached to Chromium tab `https://sysnet-org.vercel.app/onboarding`.
- Snapshot confirms onboarding shows only Step 1 sign-in form (no pre-login profile form visible).
- Ran one browser diagnostic round via CLI; gateway connection reported `device signature invalid`, per policy switched to Playwright-only.
- Added Playwright tooling in `frontend`: `@playwright/test`, `playwright.config.ts`, and `tests/smoke-auth.spec.ts`.
- Ran smoke auth suite against production (`BASE_URL=https://sysnet-org.vercel.app`): 3 passed, 1 skipped (`E2E_MAGIC_LINK_URL` not provided).
- Added Playwright API auth E2E test: `frontend/tests/auth-password-api.spec.ts`.
- Added seed script: `frontend/scripts/e2e-seed-user.mjs` and npm scripts `e2e:seed-user`, `e2e:auth`.
- Ran test with env loaded from `.env`: `1 passed` (`password auth API flow works (seed user + token)`).
